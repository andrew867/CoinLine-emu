// SPDX-License-Identifier: GPL-2.0-or-later

#include <iostream>

int main()
{
	// Placeholder: timer register semantics live in MAME z180_device; board tests verify traces via harness runs.
	// Keep executable so CMake wiring stays ready when cycle-accurate PRT asserts are added.
	return 0;
}
