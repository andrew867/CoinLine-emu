# Voiceware ROM helpers

`extract-voiceware-rom` prints size and the first 64 bytes as hex. Full Voiceware blob/sample decoding (muCCC-style opcodes) is not bundled here yet; the emulator + traces remain the authority for playback behavior.
